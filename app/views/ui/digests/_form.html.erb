<%= form_for(@digest) do |f| %>
  <% if @digest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@digest.errors.count, "error") %> prohibited this digest from being saved:</h2>

      <ul>
      <% @digest.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
